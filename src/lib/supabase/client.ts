// Supabase browser client — initialized in Phase 3 when Auth UI is built.
// Placeholder to establish the file structure.

export function getSupabaseBrowserClient(): never {
  throw new Error("Supabase browser client not yet initialized (Phase 3)");
}
