// Supabase server client — initialized in Phase 3 when Auth is built.
// Placeholder to establish the file structure.

export function getSupabaseServerClient(): never {
  throw new Error("Supabase server client not yet initialized (Phase 3)");
}
