// Supabase admin client (service role) — used by workers.
// Placeholder to establish the file structure.

export function getSupabaseAdminClient(): never {
  throw new Error("Supabase admin client not yet initialized (Phase 3)");
}
